In this app we have to give a heading call as barter and a sub heading call as a trading method. 
In this app that should be two buttons one for login and one for signup at the bottom arranged one by one and also that
 should be two input boxes one for user name and other for password. 
At the top that should be an image to make the app more good to see